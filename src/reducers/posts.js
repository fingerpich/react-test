const initialState = {
    posts: [],
    isFetching: false,
    errorFetching: null,
};

export default (state = initialState, action) => {
    switch (action.type) {
        default: return state;
    }
}
